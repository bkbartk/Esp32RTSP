# Ignore local config (copy `main/config.h.example` -> `main/config.h`)
main/config.h

# Common ESP-IDF / build artifacts
build/
CMakeLists.txt.user
cmake_build/
# sdkconfig is tracked in the repo; keep sdkconfig.old ignored
# (remove plain `sdkconfig` so it is no longer ignored)
sdkconfig.old
sdkconfig.old
*.cmake
*.ninja

# VSCode
.vscode/

# Partition and ELF outputs
*.elf
*.bin
*.map

# Serial/flasher args
flash_args
flasher_args.json

managed_components/
dependencies.lock
