// Copy this file to `main/config.h` and set your values.

// WiFi credentials (used by the project)
#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"

// Default OTA URL used when triggering OTA without specifying a URL.
#define OTA_URL_DEFAULT "http://example.com/firmware.bin"
#define ENABLE_OTA 0 //Removed for now
#define ENABLE_MOTION 0
// mDNS hostname for the device (used as <name>.local). Example: "espressif"
// Leave blank to skip mDNS initialization.
#define MDNS_NAME "espressif"

#define MQTT_URI "homeassistant.local"
#define MQTT_USERNAME "mqtt_user"
#define MQTT_PASSWORD "mqtt_pass"
